/************************************************
 * 求出一个数的阶乘的结果中后面连续的零的个数  **
 * *********************************************/
int countZeros(int n) {
	int count = 0;
	for(int i=5, m; i<=n; i+=5) {
		m = i;
		while(0 == m%5) {
			++count;
			m /= 5;
		}
	}

	return count;
}
